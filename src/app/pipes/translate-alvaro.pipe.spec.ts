import { TranslateAlvaroPipe } from './translate-alvaro.pipe';

describe('TranslateAlvaroPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateAlvaroPipe();
    expect(pipe).toBeTruthy();
  });
});
